#include <stdio.h>

/*Escreva um Algoritmo que imprima todos os números inteiros de 0 a 50.*/

int main(){
    
    int cont = 0;

    while (cont <= 50) {
        printf("%d\n", cont);
        cont++;
    }

    return 0;
    
}